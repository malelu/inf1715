
enum token {
	CHAR = 256,
	INT, 
	FLOAT, 
	WHILE, 
	IF, 
	ELSE, 
	NEW, 
	RETURN, 
	VOID,
	BOOL,
	TRUE,
	FALSE, 
	GREATER_EQUAL,
	LESS_EQUAL, 
	EQUAL_EQUAL,
	NOT_EQUAL, 
	AND, 
	OR,
	OPEN_PARENTHESIS,
	CLOSE_PARENTHESIS, 
	STRING,
	ERROR 
}


