#include <stdio.h>

enum token {
	CHAR = 256,
	INT, 
	FLOAT, 
	WHILE, 
	IF, 
	ELSE, 
	NEW, 
	RETURN, 
	VOID,
	BOOL,
	TRUE,
	FALSE, 
	GE,
	LE, 
	EE,
	NE, 
	AND, 
	OR,
	OPEN_PARENTHESIS,
	CLOSE_PARENTHESIS, 
	STRING,
	ERROR 
}


